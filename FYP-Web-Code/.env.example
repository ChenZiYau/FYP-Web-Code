# OptiPlan Environment Configuration
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.

# Database
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=optiplan_db

# Google Calendar API (server-side only — never expose to client)
GOOGLE_API_KEY=your-google-api-key-here
GOOGLE_CALENDAR_ID=your-calendar-id@gmail.com

# Admin seed password (change after first login!)
ADMIN_SEED_PASSWORD=change-me-on-first-run
